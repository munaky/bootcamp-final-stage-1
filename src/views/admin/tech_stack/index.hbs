<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <form action="/admin/tech-stack/create" method="post" enctype="multipart/form-data">
        <div>
            <label for="">name:</label>
            <input type="text" name="name" required class="px-2 rounded text-slate-600 bg-slate-200">
        </div>
        <div>
            <label for="">image:</label>
            <input type="file" name="image" required>
        </div>
        <button type="submit" class="w-[100px] px-2 py-1 rounded-lg text-center font-bold text-white bg-blue-500">submit</button>
    </form>
    <div class="flex flex-wrap gap-6 mt-4">
        {{#each tech_stacks}}
        <div class="flex flex-col items-center">
            <img src="{{this.image}}" alt="tech" class="w-[40px] h-[40px]">
            <p class="mb-4">{{this.name}}</p>
            <a href="/admin/tech-stack/edit/{{this.id}}" class="mb-1 w-[100px] px-2 py-1 rounded-lg text-center font-bold text-white bg-green-500">edit</a>
            <form action="/admin/tech-stack/remove" method="post">
                <input type="hidden" name="id" value="{{this.id}}">
                <button type="submit" class="w-[100px] px-2 py-1 rounded-lg text-center font-bold text-white bg-red-500">delete</button>
            </form>
        </div>
        {{/each}}
    </div>
</body>

</html>